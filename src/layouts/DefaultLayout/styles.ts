import styled from 'styled-components'

export const LayoutContainer = styled.div`
  max-width: 74rem;
  height: calc(100vh - 10rem);Add commentMore actions
  margin: 5rem auto;
  padding: 2.5rem;

  background: ${(props) => props.theme['gray-800']};Add commentMore actions
  border-radius: 8px;

  display: flex;Add commentMore actions
  flex-direction: column;
`
